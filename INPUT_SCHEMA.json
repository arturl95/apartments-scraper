{
    "title": "Url adder",
    "type": "object",
    "schemaVersion": 1,
    "properties": {
        "startUrls": {
            "title": "Search URLs",
            "type": "array",
            "description": "Search URLs",
            "prefill": [
                {
                    "url": "https://www.apartments.com/hollywood-hills-los-angeles-ca"
                }
            ],
            "editor": "requestListSources"
        },
        "proxyCountryCode": {
            "title": "Proxy Country Code",
            "type": "string",
            "description": "Specify the two-letter country code for remote proxies",
            "default": "FR",
            "maxLength": 2,
            "editor": "textfield"
        }
    },
    "required": ["startUrls"]
}
